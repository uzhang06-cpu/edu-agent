/**
 * ╔══════════════════════════════════════════════════════════════════╗
 * ║              🔑  星光教育 AI 后端 — 全局配置文件                  ║
 * ╠══════════════════════════════════════════════════════════════════╣
 * ║  修改 API Key 的方式有两种（推荐方式 1）：                         ║
 * ║                                                                  ║
 * ║  方式 1 ✅ 推荐：创建 .env 文件，写入：                           ║
 * ║    DEEPSEEK_API_KEY=sk-你的真实key                               ║
 * ║                                                                  ║
 * ║  方式 2 直接改代码（第 20 行）：                                  ║
 * ║    DEEPSEEK_API_KEY: 'sk-你的真实key'                            ║
 * ╚══════════════════════════════════════════════════════════════════╝
 */

require('dotenv').config();

module.exports = {

  // ──────────────────────────────────────────────────────────
  //  🔑 DeepSeek API Key  ← 在这里或 .env 文件中修改
  // ──────────────────────────────────────────────────────────
  DEEPSEEK_API_KEY: process.env.DEEPSEEK_API_KEY || 'sk-在这里填写你的DeepSeekKey',

  // 学校名称（显示在前端左上角 + 浏览器标题）
  SCHOOL_NAME: process.env.SCHOOL_NAME || '星光教育',

  // 服务端口
  PORT: process.env.PORT || 3000,

  // ──────────────────────────────────────────────────────────
  //  DeepSeek API 参数
  // ──────────────────────────────────────────────────────────
  deepseek: {
    baseURL: 'https://api.deepseek.com/v1',
    model:   'deepseek-chat',        // 或 deepseek-reasoner（R1）
    maxTokens: {
      perceive: 300,   // 感知步骤：更短的JSON
      plan:     300,   // 规划步骤：更短的策略
      execute:  500,   // 执行步骤：更简练的回复
      review:   300,   // 复盘步骤：快速评分
      refine:   500,   // 优化步骤：简练重写
    },
    temperature: {
      perceive: 0.3,   // 感知：偏确定性
      plan:     0.4,
      execute:  0.7,   // 执行：偏创意
      review:   0.2,   // 质检：严格
      refine:   0.6,
    },
  },

  // ──────────────────────────────────────────────────────────
  //  Agent 流水线控制
  // ──────────────────────────────────────────────────────────
  agent: {
    reviewScoreThreshold: 7,  // 低于此分数触发优化（0-10）
    maxRefinements: 1,        // 最多优化几次
  },

  // ──────────────────────────────────────────────────────────
  //  RAG 知识库配置
  // ──────────────────────────────────────────────────────────
  rag: {
    enabled: true,   // 是否启用知识库检索
    topK: 3,         // 每次检索返回最相关的 K 个片段
    minScore: 0.15,  // 相似度阈值（低于此值忽略该片段）
  },

};
